# Frontend — Retratos de Bonito

Pequeno frontend Next.js (App Router) para explorar a API pública.

Para rodar localmente:

```bash
cd frontend
npm install
NEXT_PUBLIC_API_URL=http://localhost:8000 npm run dev
```

Deploy recomendado: Vercel.
